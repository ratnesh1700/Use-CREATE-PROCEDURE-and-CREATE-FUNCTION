sql
CREATE FUNCTION dbo.GetCustomerName (@CustomerID INT)
RETURNS NVARCHAR(100)
AS
BEGIN
    DECLARE @CustName NVARCHAR(100);
    SELECT @CustName = CustomerName FROM Customers WHERE CustomerID = @CustomerID;
    RETURN @CustName;
END;
To use in a query:

sql
SELECT dbo.GetCustomerName(1);
